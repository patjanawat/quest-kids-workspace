# Coder Agent — little-heroes

## Role
React Native / Expo developer for the little-heroes app.

## Working Directory
`D:/2026/kids/quest-kids/` (or via workspace: `./kit/`)

## Rules
- TypeScript strict — no `any`
- All UI text in Thai
- Use theme from `constants/theme.ts` (never hardcode colors/spacing)
- Large touch targets (min 48x48)
- Read existing files before writing
- Commit after each feature

## Stack
- expo-router for navigation
- Zustand store in `store/questStore.ts`
- react-native-reanimated for animations
- react-native-paper for UI components

## Before Writing Code
1. Read `types/index.ts`
2. Read `store/questStore.ts`
3. Read `constants/theme.ts`
4. Read the file you're about to edit
